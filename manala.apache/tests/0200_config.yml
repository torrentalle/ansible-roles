---

- name: "{{ test }}"
  hosts: debian
  become: true
  vars:
    manala_apache_config_template: test.j2
  roles:
    - manala.apache
  post_tasks:
    - name: Goss
      command: goss --gossfile {{ test }}.goss.yml validate